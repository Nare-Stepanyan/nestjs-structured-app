{
  "name": "default",
  "type": "postgres",
  "host": "postgres",
  "username": "preezma",
  "password": "secret",
  "database": "db_eatnact",
  "port": 5432,
  "logging": true,
  "entities": ["src/**/*.entity.ts"],
  "migrations": ["migrations/*.ts"],
  "migrationsRun": true,
  "synchronize": false,
  "cli": {
    "entitiesDir": "src",
    "migrationsDir": "migrations"
  }
}
